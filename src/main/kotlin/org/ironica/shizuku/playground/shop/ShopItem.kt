package org.ironica.shizuku.playground.shop

import org.ironica.shizuku.playground.Size

data class Weapon(val id: Int, val atk: Int, val cost: Int)

data class Portion(val size: Size, val cost: Int)
