package org.ironica.shizuku.playground.tiles

enum class Block {
    NONE,
    OPEN,
    BLOCKED, MOUNTAIN, STONE,
    WATER, LAVA,
    TREE, DESERT, HOME,
    LOCK
}

enum class Item {
    NONE, GEM, CLOSEDSWITCH, OPENEDSWITCH, BEEPER, PORTAL, PLATFORM
}

